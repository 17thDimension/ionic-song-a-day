html
  body
    ion-view(view-title='Songs')
      ion-content(has-header='true', has-tabs='true')
        ion-list
          ion-item(ng-repeat='song in songs', type='item-text-wrap', href='#/tab/song/{{song.id}}')
            h3 {{song.title}} 
            p {{song.artist.alias}}
